# Seiko manufacturer-page replay fixtures

HTML snapshots of official US product pages used when `LOOKUP_PROVIDER=replay`. The service parses these files; they are not returned as fake live HTTP results.

Images used during replay live in `fixtures/seiko_images/originals/` (see that README for usage notes).
