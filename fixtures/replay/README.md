Replay fixtures live alongside `../supplier_catalog.csv` and `../images/`.

Replay mode executes the same Python services and tools as live mode without calling Bedrock.
Image classifications for known filenames are labeled `fixture_replay` in product evidence.
